# ============================================================
# SaaS Telegram CRM-бот — зависимости
# ============================================================

# ── FastAPI + Server ──
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
jinja2>=3.1.0

# ── База данных ──
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
alembic>=1.13.0
psycopg2-binary>=2.9.9

# ── Redis ──
redis[hiredis]>=5.0.0

# ── Валидация / Настройки ──
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0

# ── Безопасность ──
cryptography>=42.0.0
python-jose[cryptography]>=3.3.0

# ── Telegram Bot ──
aiogram>=3.10.0
aiohttp[speedups]>=3.9.0
aiohttp-socks>=0.9.0

# ── HTTP-клиент ──
httpx>=0.27.0

# ── YooKassa ──
yookassa>=3.2.0

# ── Логирование ──
python-json-logger>=2.0.0

# ── Мониторинг ──
prometheus-client>=0.20.0

# ── Тестирование ──
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0
